<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>ShinyBelle – Timeless Elegance</title>

  <!-- Fonts & Icons -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&family=Lato:wght@300;400;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <style>
    :root {
      --accent-pink: #f7e6f2;
      --accent-pink-dark: #b86fa7;
      --accent-gold: #bfa05a;
      --text-dark: #18141c;
      --soft-gray: #f8f6fa;
      --white: #fff;
      --shadow: 0 4px 24px rgba(184, 111, 167, 0.10), 0 1.5px 6px rgba(0,0,0,0.06);
      --radius: 20px;
      --transition: all .32s cubic-bezier(.4,0,.2,1);
      --max-width: 1200px;
    }
    * { margin: 0; padding: 0; box-sizing: border-box; }
    html { font-size: 16px; scroll-behavior: smooth; }
    body {
      font-family: 'Lato', sans-serif;
      color: var(--text-dark);
      background: var(--soft-gray);
      line-height: 1.7;
      letter-spacing: 0.01em;
      min-height: 100vh;
    }
    img, video { max-width: 100%; display: block; border-radius: var(--radius); }
    a {
      text-decoration: none;
      color: var(--accent-pink-dark);
      font-weight: 600;
      transition: color .2s;
    }
    a:hover, a:focus {
      color: var(--accent-gold);
    }
    h1,h2,h3,h4 {
      font-family: 'Playfair Display', serif;
      color: var(--text-dark);
      font-weight: 700;
      letter-spacing: 0.02em;
      text-shadow: 0 1.5px 6px rgba(184, 111, 167, 0.08);
    }
    h1 { font-size: 3.2rem; line-height: 1.1; margin-bottom: 1.2rem; }
    h2 { font-size: 2rem; margin-bottom: 1rem; }
    h3 { font-size: 1.2rem; }
    p { margin-bottom: 1.1rem; font-size: 1.08rem; }
    .container {
      width: 92%;
      max-width: var(--max-width);
      margin: 0 auto;
      display: flex;
      flex-direction: column;
    }
    .btn {
      display: inline-block;
      padding: .85rem 2.2rem;
      background: linear-gradient(90deg, var(--accent-pink-dark) 0%, var(--accent-gold) 100%);
      color: #fff;
      border-radius: 999px;
      text-transform: uppercase;
      font-weight: 700;
      letter-spacing: 1.5px;
      font-size: 1.08rem;
      box-shadow: 0 2px 12px rgba(184, 111, 167, 0.13);
      border: none;
      transition: var(--transition);
      position: relative;
      overflow: hidden;
    }
    .btn:hover, .btn:focus {
      background: linear-gradient(90deg, var(--accent-gold) 0%, var(--accent-pink-dark) 100%);
      color: #fff;
      box-shadow: 0 6px 24px rgba(184, 111, 167, 0.18);
      transform: translateY(-2px) scale(1.04);
    }
    header {
      position: sticky;
      top: 0;
      background: #fff;
      box-shadow: 0 2px 12px rgba(184, 111, 167, 0.08);
      z-index: 1000;
    }
    .navbar {
      display: flex;
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
      padding: 1.2rem 0;
      width: 100%;
      position: relative;
      gap: 2rem;
    }
    .logo {
      display: flex;
      flex-direction: row;
      align-items: center;
      min-width: 0;
      position: relative;
    }
    .logo-text {
      font-family: 'Playfair Display', serif;
      font-size: 2.1rem;
      font-weight: 700;
      letter-spacing: 0.07em;
      color: var(--text-dark);
      display: flex;
      align-items: center;
    }
    .logo-accent {
      color: var(--accent-gold);
      margin-left: 0.12em;
      font-family: inherit;
      font-size: inherit;
      font-weight: inherit;
      letter-spacing: inherit;
      text-shadow: 0 1.5px 6px rgba(231, 207, 164, 0.10);
    }
    .logo-underline {
      display: block;
      width: 70%;
      height: 3px;
      margin-left: 0.1em;
      margin-top: 0.2em;
      border-radius: 2px;
      background: linear-gradient(90deg, var(--accent-gold) 0%, var(--accent-pink-dark) 100%);
      opacity: 0.5;
      position: absolute;
      left: 0;
      bottom: -7px;
      pointer-events: none;
    }
    nav {
      display: flex;
      flex: 1;
      justify-content: center;
      align-items: center;
      gap: 1.5rem;
    }
    nav a {
      position: relative;
      font-size: 1.08rem;
      font-weight: 600;
      color: var(--text-dark);
      padding-bottom: 2px;
      transition: color .3s;
    }
    nav a::after {
      content: '';
      position: absolute;
      left: 0; bottom: -4px;
      width: 0; height: 2.5px;
      background: linear-gradient(90deg, var(--accent-pink-dark), var(--accent-gold));
      border-radius: 2px;
      transition: width .35s cubic-bezier(.4,0,.2,1);
    }
    nav a:hover, nav a:focus {
      color: var(--accent-pink-dark);
    }
    nav a:hover::after, nav a:focus::after {
      width: 100%;
    }
    .navbar-action {
      display: flex;
      align-items: center;
      gap: 0.7rem;
      z-index: 2;
    }
    .login-btn {
      padding: 0.6rem 1.5rem;
      border-radius: 999px;
      border: 2px solid var(--accent-pink-dark);
      background: #fff;
      color: var(--accent-pink-dark);
      font-family: 'Lato', sans-serif;
      font-size: 1.05rem;
      font-weight: 700;
      letter-spacing: 1px;
      transition: background 0.25s, color 0.25s, box-shadow 0.25s, border-color 0.25s;
      box-shadow: 0 1.5px 6px rgba(184, 111, 167, 0.10);
      outline: none;
    }
    .login-btn:hover, .login-btn:focus {
      background: linear-gradient(90deg, var(--accent-pink-dark) 0%, var(--accent-gold) 100%);
      color: #fff;
      border-color: var(--accent-gold);
      box-shadow: 0 4px 16px rgba(191, 160, 90, 0.13);
      text-decoration: none;
    }
    .hero {
      position: relative;
      background: #fff url('https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=1600&q=80') center/cover no-repeat;
      height: 70vh;
      min-height: 420px;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      color: var(--text-dark);
      border-radius: 0 0 var(--radius) var(--radius);
      box-shadow: var(--shadow);
      margin-bottom: 2.5rem;
      overflow: hidden;
    }
    .hero::before {
      content: '';
      position: absolute;
      inset: 0;
      background: rgba(247, 230, 242, 0.72);
      border-radius: 0 0 var(--radius) var(--radius);
    }
    .hero-content {
      position: relative;
      max-width: 700px;
      padding: 0 1.5rem;
      z-index: 1;
    }
    .hero-content h1 {
      font-size: 3.5rem;
      text-transform: uppercase;
      letter-spacing: 4px;
      margin-bottom: 1.2rem;
      color: var(--accent-pink-dark);
      text-shadow: 0 2px 12px rgba(184, 111, 167, 0.13);
    }
    .hero-content .btn {
      margin-top: 1.2rem;
      font-size: 1.15rem;
    }
    .categories {
      background: #fff;
      padding: 3.5rem 0 2.5rem 0;
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      margin-bottom: 2.5rem;
    }
    .categories-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      gap: 2.2rem;
      padding: 0 1.5rem;
      background: var(--accent-pink);
      border-radius: var(--radius);
    }
    .category-card {
      position: relative;
      overflow: hidden;
      border-radius: var(--radius);
      background: #fff;
      transition: var(--transition);
      box-shadow: 0 2px 12px rgba(184, 111, 167, 0.10);
      cursor: pointer;
      border: 2px solid var(--accent-pink-dark);
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 220px;
    }
    .category-card img {
      transition: transform .4s cubic-bezier(.4,0,.2,1);
      border-radius: var(--radius) var(--radius) 0 0;
      min-height: 120px;
      object-fit: cover;
      width: 100%;
      aspect-ratio: 1.2/1;
    }
    .category-card:hover img {
      transform: scale(1.08) rotate(-1deg);
      filter: brightness(1.08) saturate(1.1);
    }
    .category-card:hover {
      transform: translateY(-7px) scale(1.03);
      box-shadow: 0 8px 32px rgba(191, 160, 90, 0.13);
      border-color: var(--accent-gold);
    }
    .category-card .label {
      text-align: center;
      padding: 1.1rem 0 .9rem 0;
      font-weight: 800;
      font-size: 1.13rem;
      background: #fff;
      color: var(--accent-pink-dark);
      letter-spacing: 1.2px;
      border-radius: 0 0 var(--radius) var(--radius);
      width: 100%;
    }
    .featured {
      padding: 3.5rem 0 2.5rem 0;
      background: #fff;
      border-radius: var(--radius);
      box-shadow: var(--shadow);
    }
    .featured h2 {
      text-align: center;
      margin-bottom: 2.2rem;
      color: var(--accent-pink-dark);
      letter-spacing: 2px;
    }
    .products-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: 2.2rem;
      padding: 0 1.5rem;
    }
    .product-card {
      background: #fff;
      border-radius: var(--radius);
      box-shadow: 0 2px 12px rgba(184, 111, 167, 0.10);
      text-align: center;
      padding: 2.2rem 1.2rem 1.5rem 1.2rem;
      transition: var(--transition);
      border: 2px solid var(--accent-pink-dark);
      position: relative;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 320px;
    }
    .product-card img {
      width: 100%;
      aspect-ratio: 1.2/1;
      object-fit: cover;
      border-radius: var(--radius) var(--radius) 0 0;
      margin-bottom: 1.2rem;
    }
    .product-card:hover {
      transform: translateY(-7px) scale(1.03);
      box-shadow: 0 8px 32px rgba(191, 160, 90, 0.13);
      border-color: var(--accent-gold);
    }
    .product-card h4 {
      margin: 1.2rem 0 .7rem 0;
      font-size: 1.18rem;
      color: var(--accent-pink-dark);
      font-weight: 800;
    }
    .product-card span {
      color: var(--accent-gold);
      font-weight: 800;
      font-size: 1.13rem;
      letter-spacing: 1px;
    }
    footer {
      border-top: 1.5px solid #f3e2ec;
      padding: 2.5rem 0 1.5rem 0;
      background: #fff;
      border-radius: var(--radius) var(--radius) 0 0;
      box-shadow: 0 -2px 12px rgba(184, 111, 167, 0.08);
      margin-top: 3rem;
    }
    .footer-contact-minimal {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 120px;
      padding: 2rem 0 1.5rem 0;
      background: #fff;
      border-radius: var(--radius) var(--radius) 0 0;
      box-shadow: 0 -2px 12px rgba(184, 111, 167, 0.08);
      margin-top: 1.5rem;
      text-align: center;
    }
    .footer-title {
      color: var(--accent-pink-dark);
      font-family: 'Playfair Display', serif;
      font-size: 1.25rem;
      font-weight: 800;
      margin-bottom: 0.7rem;
      letter-spacing: 1px;
    }
    .footer-contact-details {
      font-family: 'Lato', sans-serif;
      color: var(--text-dark);
      font-size: 1.08rem;
      font-weight: 500;
      line-height: 1.7;
      margin-bottom: 0.2rem;
    }
    @media (max-width: 1024px) {
      .hero-content h1 { font-size: 2.2rem; }
      .container { width: 97%; }
      .navbar { gap: 1.2rem; }
      .logo { min-width: 150px; }
    }
    @media (max-width: 768px) {
      .hero-content h1 { font-size: 1.7rem; }
      .navbar { flex-direction: column; gap: 1rem; align-items: flex-start; }
      .logo { margin-left: 0; align-items: flex-start; }
      nav { margin-left: 0; }
      .navbar-action { margin-left: 0; }
      .login-btn { margin-left: 0; }
      .categories, .featured { padding: 1.5rem 0 1rem 0; }
      .footer-contact-minimal { padding: 1.2rem 0 1rem 0; }
    }
    @media (max-width: 480px) {
      .hero { min-height: 220px; }
      .hero-content h1 { font-size: 1.1rem; }
      .btn { font-size: .98rem; padding: .7rem 1.2rem; }
      .modal-content { padding: 1.2rem 0.5rem 0.7rem 0.5rem; width: 98vw; min-height: 420px; }
      .navbar-action { padding-right: 0.5rem; }
    }
    /* Modal Styles */
    .modal-overlay {
      display: none;
      position: fixed;
      z-index: 2000;
      left: 0; top: 0; width: 100vw; height: 100vh;
      background: rgba(24, 20, 28, 0.18);
      justify-content: center;
      align-items: center;
      transition: opacity .3s;
    }
    .modal-overlay.active {
      display: flex;
    }
    .modal-content {
      background: #fff;
      border-radius: 22px;
      box-shadow: 0 12px 48px rgba(184, 111, 167, 0.18), 0 2px 12px rgba(24, 20, 28, 0.08);
      width: 410px;
      max-width: 98vw;
      min-height: 480px;
      padding: 3.2rem 2.5rem 2.2rem 2.5rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      position: relative;
      font-family: 'Lato', sans-serif;
      justify-content: flex-start;
      box-shadow: 0 8px 40px rgba(184, 111, 167, 0.13), 0 2px 12px rgba(24, 20, 28, 0.08);
      border: 1.5px solid #f7e6f2;
    }
    .modal-title {
      font-family: 'Playfair Display', serif;
      font-size: 2.1rem;
      font-weight: 700;
      color: var(--accent-pink-dark);
      text-align: center;
      margin-bottom: 0.7rem;
      letter-spacing: 2px;
      text-transform: uppercase;
      opacity: 0.8;
    }
    .modal-subtitle {
      font-size: 1.08rem;
      font-weight: 700;
      color: var(--text-dark);
      text-align: center;
      margin-bottom: 1.2rem;
    }
    .modal-form {
      width: 100%;
      display: flex;
      flex-direction: column;
      align-items: stretch;
      margin-bottom: 1.3rem;
    }
    .modal-label {
      font-size: 1.05rem;
      color: #888;
      font-weight: 600;
      margin-bottom: 0.2rem;
      text-align: left;
      display: block;
    }
    .modal-input-wrap {
      position: relative;
      margin-bottom: 0.8rem;
    }
    .modal-input-icon {
      position: absolute;
      left: 0.7rem;
      top: 50%;
      transform: translateY(-50%);
      color: #bbb;
      font-size: 1.1rem;
      pointer-events: none;
    }
    .modal-input {
      width: 100%;
      padding: 0.7rem 2.2rem 0.7rem 2.1rem;
      border: none;
      border-bottom: 2px solid #bbb;
      font-size: 1.08rem;
      background: transparent;
      color: var(--text-dark);
      outline: none;
      font-family: inherit;
      border-radius: 0;
      transition: border-color .2s;
    }
    .modal-input:focus {
      border-bottom: 2px solid var(--accent-pink-dark);
    }
    .modal-login-btn {
      width: 100%;
      background: #18141c;
      color: #fff;
      font-size: 1.18rem;
      font-weight: 700;
      border: none;
      border-radius: 7px;
      padding: 1.1rem 0;
      margin-top: 0.7rem;
      margin-bottom: 0.2rem;
      cursor: pointer;
      transition: background .2s;
      letter-spacing: 1px;
      box-shadow: 0 2px 8px rgba(24, 20, 28, 0.08);
    }
    .modal-login-btn:hover, .modal-login-btn:focus {
      background: var(--accent-pink-dark);
    }
    .modal-divider-wrap {
      display: flex;
      align-items: center;
      width: 100%;
      margin: 1.2rem 0 1.1rem 0;
    }
    .modal-divider {
      flex: 1;
      height: 1.5px;
      background: #e5e5e5;
      border-radius: 2px;
    }
    .modal-divider-text {
      margin: 0 0.7rem;
      color: #888;
      font-size: 1.05rem;
      font-weight: 600;
      opacity: 0.8;
    }
    .modal-social-btns {
      display: flex;
      width: 100%;
      margin-bottom: 1.5rem;
      gap: 0;
    }
    .modal-social-btns.single-btn {
      justify-content: center;
    }
    .modal-social-btn {
      width: 100%;
      background: #fff;
      border: 1.5px solid #bbb;
      border-radius: 8px;
      padding: 1.1rem 0;
      font-size: 1.13rem;
      font-weight: 700;
      color: #222;
      cursor: pointer;
      transition: border-color .2s, color .2s, box-shadow .2s;
      font-family: inherit;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 2px 8px rgba(184, 111, 167, 0.07);
      letter-spacing: 1px;
      gap: 0.7em;
    }
    .modal-social-btn.google-google-logo {
      border: 1px solid #000;
      background: #fff;
      color: #222;
      font-size: 1.15rem;
      font-weight: 500;
      border-radius: 4px;
      padding: 0.7rem 0;
      width: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: none;
      letter-spacing: 0;
      transition: background 0.18s, border-color 0.18s;
      margin-bottom: 1.5rem;
      outline: none;
    }
    .modal-social-btn.google-google-logo:hover, .modal-social-btn.google-google-logo:focus {
      background: #f2f2f2;
      border-color: #000;
    }
    .google-logo-text {
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: Arial, sans-serif;
      font-size: 1.15rem;
      font-weight: 500;
      letter-spacing: 0;
      padding: 0;
      margin: 0;
    }
    .modal-close {
      background: none;
      border: none;
      color: #18141c;
      font-size: 2.2rem;
      font-weight: 400;
      cursor: pointer;
      opacity: 0.7;
      transition: opacity .2s, color .2s;
      position: absolute;
      top: 1.1rem;
      right: 1.3rem;
      left: auto;
      bottom: auto;
      margin: 0;
      z-index: 10;
    }
    .modal-close:hover, .modal-close:focus {
      opacity: 1;
      color: var(--accent-pink-dark);
    }
    /* Product Details Modal Styles */
    #product-details-modal {
      display: none;
      position: fixed;
      z-index: 3000;
      left: 0; top: 0; width: 100vw; height: 100vh;
      background: rgba(24, 20, 28, 0.18);
      justify-content: center;
      align-items: center;
      transition: opacity .3s;
    }
    #product-details-modal.active {
      display: flex;
    }
    .product-details-modal-content {
      background: #fff;
      border-radius: 22px;
      box-shadow: 0 12px 48px rgba(184, 111, 167, 0.18), 0 2px 12px rgba(24, 20, 28, 0.08);
      width: 600px;
      max-width: 98vw;
      min-height: 420px;
      padding: 2.5rem 2.2rem 2.2rem 2.2rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      position: relative;
      font-family: 'Lato', sans-serif;
      justify-content: flex-start;
      border: 1.5px solid #f7e6f2;
      animation: fadeInUp .32s cubic-bezier(.4,0,.2,1);
    }
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(40px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .product-details-modal-layout {
      display: flex;
      flex-direction: row;
      gap: 2.2rem;
      width: 100%;
      align-items: flex-start;
    }
    .product-details-image-wrap {
      flex: 1 1 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      min-width: 0;
    }
    #product-details-img {
      width: 100%;
      max-width: 220px;
      border-radius: var(--radius);
      box-shadow: 0 4px 24px rgba(184, 111, 167, 0.13);
      background: var(--accent-pink);
      object-fit: cover;
      aspect-ratio: 1.2/1;
    }
    .product-details-info {
      flex: 1 1 50%;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: flex-start;
      gap: 1.1rem;
      min-width: 0;
    }
    .product-details-title {
      font-family: 'Playfair Display', serif;
      font-size: 1.7rem;
      font-weight: 700;
      color: var(--text-dark);
      margin-bottom: 0.2rem;
      letter-spacing: 0.04em;
      text-shadow: 0 1.5px 6px rgba(184, 111, 167, 0.08);
    }
    .product-details-price {
      color: var(--accent-gold);
      font-size: 1.18rem;
      font-weight: 800;
      margin-bottom: 0.7rem;
      letter-spacing: 1.2px;
    }
    .product-details-desc {
      font-size: 1.08rem;
      color: var(--text-dark);
      margin-bottom: 1.1rem;
      line-height: 1.7;
      opacity: 0.92;
      font-weight: 500;
      max-width: 28rem;
    }
    #product-details-add-to-cart {
      display: inline-block;
      padding: 0.8rem 2.2rem;
      background: linear-gradient(90deg, var(--accent-pink-dark) 0%, var(--accent-gold) 100%);
      color: #fff;
      border-radius: 999px;
      text-transform: uppercase;
      font-weight: 700;
      letter-spacing: 1.5px;
      font-size: 1.08rem;
      box-shadow: 0 2px 12px rgba(184, 111, 167, 0.13);
      border: none;
      transition: var(--transition);
      position: relative;
      overflow: hidden;
      margin-top: 0.7rem;
    }
    #product-details-add-to-cart:hover, #product-details-add-to-cart:focus {
      background: linear-gradient(90deg, var(--accent-gold) 0%, var(--accent-pink-dark) 100%);
      color: #fff;
      box-shadow: 0 6px 24px rgba(184, 111, 167, 0.18);
      transform: translateY(-2px) scale(1.04);
    }
    @media (max-width: 700px) {
      .product-details-modal-layout {
        flex-direction: column;
        gap: 1.2rem;
        align-items: center;
      }
      .product-details-image-wrap { justify-content: center; }
      #product-details-img { max-width: 90vw; }
    }
  </style>
</head>
<body>
  <header>
    <div class="container navbar">
      <div class="logo">
        <span class="logo-text">Shiny<span class="logo-accent">Belle</span></span>
        <span class="logo-underline"></span>
      </div>
      <nav>
        <a href="#">Home</a>
        <a href="#rings">Rings</a>
        <a href="#earrings">Earrings</a>
        <a href="#necklaces">Necklaces</a>
        <a href="#bracelets">Bracelets</a>
        <a href="#charms">Charms</a>
        <a href="#new">New Collection</a>
      </nav>
      <div class="navbar-action">
        <a href="#login" class="login-btn">Log In</a>
      </div>
    </div>
  </header>
  <section class="hero">
    <div class="hero-content">
      <h1>Timeless Elegance<br/>Effortless Style</h1>
      <a href="#shop" class="btn">Shop Now</a>
    </div>
  </section>
  <div class="container">
    <section class="categories">
      <div class="categories-grid">
        <div class="category-card">
          <img src="https://img.freepik.com/premium-photo/gold-jewelry-white-background_237502-490.jpg" alt="Jewelry">
          <div class="label">Jewelry</div>
        </div>
        <div class="category-card">
          <img src="https://media.istockphoto.com/id/1363767151/photo/yellow-leather-bag-of-isolated-woman-on-white-background.jpg?s=612x612&w=0&k=20&c=X7PcXbPp99t0xAFbWLV7Yfm3YzsFSugtfEGZ0x2aCjo=" alt="Bags">
          <div class="label">Bags</div>
        </div>
        <div class="category-card">
          <img src="https://img.freepik.com/premium-photo/elegant-women-s-summer-suede-red-stilettos-new-pair-shoes-white-background_400928-1689.jpg?w=360" alt="Shoes">
          <div class="label">Shoes</div>
        </div>
       
      
      </div>
    </section>
    <section class="featured">
      <h2>Our Most Loved Items:</h2>
      <div class="products-grid">
        <div class="product-card" data-title="Diamond Ring" data-price="$335" data-img="https://images.unsplash.com/photo-1464983953574-0892a716854b?auto=format&fit=crop&w=400&q=80" data-desc="A dazzling diamond ring that captures the light and the heart. Crafted for timeless elegance and everyday luxury. Perfect for special occasions or daily wear.">
          <img src="https://images.unsplash.com/photo-1464983953574-0892a716854b?auto=format&fit=crop&w=400&q=80" alt="Diamond Ring">
          <h4>Diamond Ring</h4>
          <span>$335</span>
        </div>
        <div class="product-card" data-title="Silver Bracelet" data-price="$389" data-img="https://images.unsplash.com/photo-1519125323398-675f0ddb6308?auto=format&fit=crop&w=400&q=80" data-desc="A sleek silver bracelet with a modern twist. Its minimal design makes it a versatile piece for any outfit, day or night.">
          <img src="https://images.unsplash.com/photo-1519125323398-675f0ddb6308?auto=format&fit=crop&w=400&q=80" alt="Silver Bracelet">
          <h4>Silver Bracelet</h4>
          <span>$389</span>
        </div>
        <div class="product-card" data-title="Infinity Necklace" data-price="$335" data-img="https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=400&q=80" data-desc="An elegant infinity necklace symbolizing endless love and beauty. Delicate, refined, and perfect for layering.">
          <img src="https://images.unsplash.com/photo-1506744038136-46273834b3fb?auto=format&fit=crop&w=400&q=80" alt="Infinity Necklace">
          <h4>Infinity Necklace</h4>
          <span>$335</span>
        </div>
        <div class="product-card" data-title="Stud Earrings" data-price="$195" data-img="https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=400&q=80" data-desc="Classic stud earrings that add a touch of sparkle to any look. Lightweight, comfortable, and always in style.">
          <img src="https://images.unsplash.com/photo-1517841905240-472988babdf9?auto=format&fit=crop&w=400&q=80" alt="Stud Earrings">
          <h4>Stud Earrings</h4>
          <span>$195</span>
        </div>
      </div>
    </section>
  </div>
  <footer>
    <div class="footer-contact-minimal">
      <h3 class="footer-title">Contact Us</h3>
      <div class="footer-contact-details">
        <div>Email: example@example.com</div>
        <div>Phone: 000-000-0000</div>
      </div>
    </div>
  </footer>
  <div id="login-modal" class="modal-overlay">
    <div class="modal-content">
      <div class="modal-title">Welcome Back</div>
      <div class="modal-subtitle">Sign in to your account</div>
      <form class="modal-form" autocomplete="off" onsubmit="return false;">
        <label class="modal-label" for="modal-phone">Phone</label>
        <div class="modal-input-wrap">
          <span class="modal-input-icon"><i class="fa fa-info-circle"></i></span>
          <input type="tel" id="modal-phone" class="modal-input" placeholder="Enter your phone number..." />
        </div>
        <button class="modal-login-btn" type="submit">Log In</button>
      </form>
      <div class="modal-divider-wrap">
        <div class="modal-divider"></div>
        <span class="modal-divider-text">or</span>
        <div class="modal-divider"></div>
      </div>
      <div class="modal-social-btns single-btn">
        <button class="modal-social-btn google-google-logo" type="button">
          <span class="google-logo-text">
            <span style="color:#4285F4">G</span><span style="color:#EA4335">o</span><span style="color:#FBBC05">o</span><span style="color:#4285F4">g</span><span style="color:#34A853">l</span><span style="color:#EA4335">e</span>
          </span>
        </button>
      </div>
      <button class="modal-close" id="modal-close-btn">&times;</button>
    </div>
  </div>
  <!-- Product Details Modal (hidden by default) -->
  <div id="product-details-modal" class="modal-overlay">
    <div class="modal-content product-details-modal-content">
      <button class="modal-close" id="product-details-close">&times;</button>
      <div class="product-details-modal-layout">
        <div class="product-details-image-wrap">
          <img id="product-details-img" src="" alt="Product Image" />
        </div>
        <div class="product-details-info">
          <div class="product-details-title" id="product-details-title"></div>
          <div class="product-details-price" id="product-details-price"></div>
          <div class="product-details-desc" id="product-details-desc"></div>
          <button class="add-to-cart-btn" id="product-details-add-to-cart"><i class="fa fa-shopping-cart"></i> Add to Cart</button>
        </div>
      </div>
    </div>
  </div>
  <script>
    // Modal logic
    const loginBtn = document.querySelector('.login-btn');
    const modal = document.getElementById('login-modal');
    const closeModalBtn = document.getElementById('modal-close-btn');
    loginBtn.addEventListener('click', function(e) {
      e.preventDefault();
      modal.classList.add('active');
    });
    closeModalBtn.addEventListener('click', function() {
      modal.classList.remove('active');
    });
    modal.addEventListener('click', function(e) {
      if (e.target === modal) {
        modal.classList.remove('active');
      }
    });
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape') {
        modal.classList.remove('active');
      }
    });

    // Product card click logic (update to show modal)
    const productCards = document.querySelectorAll('.product-card');
    const productDetailsModal = document.getElementById('product-details-modal');
    const productDetailsClose = document.getElementById('product-details-close');
    const productDetailsImg = document.getElementById('product-details-img');
    const productDetailsTitle = document.getElementById('product-details-title');
    const productDetailsPrice = document.getElementById('product-details-price');
    const productDetailsDesc = document.getElementById('product-details-desc');

    productCards.forEach(card => {
      card.style.cursor = 'pointer';
      card.addEventListener('click', function() {
        productDetailsTitle.textContent = card.getAttribute('data-title');
        productDetailsPrice.textContent = card.getAttribute('data-price');
        productDetailsImg.src = card.getAttribute('data-img');
        productDetailsImg.alt = card.getAttribute('data-title');
        productDetailsDesc.textContent = card.getAttribute('data-desc');
        productDetailsModal.classList.add('active');
        document.body.style.overflow = 'hidden'; // Prevent background scroll
      });
    });
    productDetailsClose.addEventListener('click', function() {
      productDetailsModal.classList.remove('active');
      document.body.style.overflow = '';
    });
    productDetailsModal.addEventListener('click', function(e) {
      if (e.target === productDetailsModal) {
        productDetailsModal.classList.remove('active');
        document.body.style.overflow = '';
      }
    });
    document.addEventListener('keydown', function(e) {
      if (productDetailsModal.classList.contains('active') && e.key === 'Escape') {
        productDetailsModal.classList.remove('active');
        document.body.style.overflow = '';
      }
    });
  </script>
</body>
</html> 
